# 변경 이력 (HISTORY)

`README.md`는 **지금 되는 것**만 적습니다. 왜 이렇게 바뀌었는지, 어떤 실측으로 결정됐는지는 여기 남깁니다.
판정도·측정치(−6.3세, 40건, 122줄 …)는 전부 여기 있습니다. 맨 아래는 커밋 한 줄 연대기.

## 항목별 사정

### 닮은 캐릭터 태그 — Danbooru 실측 DB

주인공 얼굴이 회차를 넘어 흔들리는 문제는 프롬프트 문장을 아무리 정리해도 고정되지 않습니다.
대신 **학습량이 확인된 캐릭터 태그**를 하나 빌려 씁니다. 단, 사람이 고르지 않고
**시트 속성(머리색·머리길이·눈동자·나이대·피부)**으로 고릅니다.

- 재료: `data/chara_tags.yaml` — 여성 캐릭터 90여 종의 속성 분포를 태그 게시판 실측으로 채운 데이터입니다.
  ("이 캐릭터는 갈색 머리"를 사람이 기억으로 쓰면 틀립니다. 같은 태그가 붙은 글에서
  해당 속성 태그가 몇 % 함께 붙는지를 측정해 넣었습니다 — 모델이 실제로 무엇을 봤는지가 기준입니다.)
- 선택: 요청 속성과의 가중 유사도(머리색 .35 · 길이 .20 · 스타일 .15 · 눈 .10 · 나이대 .10 · 피부 .05).
  **최고 유사도가 0.50 아래면 태그를 쓰지 않습니다.** 없는 얼굴을 억지로 빌리면 원작 캐릭터가 섞입니다.
- 안전장치: 글 수 600 미만은 후보에서 제외(얼굴이 아니라 그림체로 샙니다) · 성인 콘텐츠 비율 60% 초과 제외
  · 아동 코드 캐릭터는 시트가 어린 체형을 요구할 때만 후보가 됩니다(나이대 태그와 성인 징후가 만나면 화면이 갈라집니다)
  · **주인공 1명만** 대상으로 합니다(상대방은 계속 실루엣 — 캐릭터 태그는 머리와 복장을 통째로 끌어옵니다).
- 우선순위: 시트에 `#…#` 을 직접 쓰시면 그게 무조건 앞섭니다. 자동 선택은 빈 칸을 채우는 역할입니다.
- 작품(저작권) 태그는 기본 OFF입니다(`--char-series`). 캐릭터 태그 옆에 작품 태그가 서면 화풍이 그 작품으로
  끌려가서 화풍 LoRA와 싸울 수 있습니다. 붙이실 때는 한 회차만 켜서 비교해 보시기 바랍니다.

```
venv/bin/python run_comic.py --ep 1                  # 기본: 자동 선택 ON (시트에 #태그#가 없을 때만)
venv/bin/python run_comic.py --ep 1 --char-tag off   # 속성 태그만 사용
venv/bin/python run_comic.py --ep 1 --char-series    # 작품 태그도 같이 (화풍 비교용)
```

**실제로 효과가 있었나** — 시트 3종 × 3조건(속성만 / +캐릭터 / +캐릭터+작품)을 같은 시드로 뽑아
VLM에게 "같은 인물인가"를 물었습니다. 교차-시드는 같은 컷을 시드 두 번 뽑아 비교한 것입니다
(회차가 바뀌면 시드도 바뀌므로, 이 표가 "회차를 넘어 같은 얼굴인가"의 대리 시험입니다).

| 시트 | 속성만 | +캐릭터 태그 | +작품 태그까지 |
|---|---|---|---|
| 28세 · 갈색 긴머리 | 2/2 · 9.0 | 2/2 · 9.0 | 2/2 · 9.0 |
| 38세 · 흑발 업스타일(미시) | **1/2 · 7.5** | **2/2 · 9.0** | 1/2 · 7.5 |
| 42세 · 흑발 웨이브+안경 | **1/2 · 7.5** | **2/2 · 8.5** | 2/2 · 9.0 |

(좌우 순서대로 "같은 인물 판정에 몇 번 통과했나 · 유사도 평균(0~10)". 4구도 판정은 속성만 팔에서 한 건 NO, 나머지 전부 YES였습니다.)

읽는 방법: 생김새가 속성 태그로 이미 단단히 고정되는 회차(1행)는 캐릭터 태그가 아무 일도 하지 않습니다.
속성으로 안 잡히는 생김새(2·3행 — 검고 성숙한 축은 학습 분포 자체가 금발/젊은 쪽으로 기워 있습니다)에서는
교차-시드 판정이 NO→YES, 7.5→9.0으로 올라옵니다. 그래서 기본은 켜 둔다(이득은 있고 손해는 없습니다).
**작품 태그는 방향이 일정하지 않습니다**(2행에서 손해, 3행에서 이득) — 기본 OFF가 맞습니다.

선택이 일어나면 로그 한 줄로 남습니다 — 재현 가능한 판단이라 같은 시트는 같은 얼굴로 갑니다.
```
[[CHAR TAG]] 시트 속성( hair_color✓,hair_length✓,eyes~ ) → **yor_forger** (유사도 0.86, 학습 11443글) — 모든 컷에 강제 주입됩니다
[[CHAR TAG]] 최고 유사도 0.37 < 0.5 — 속성 태그만 사용합니다
```

### 나이대 발화 정책

서른여덟 살 시트에 `mature female`을 또렷이 넣어도 그림은 20대 중반이었습니다. 원인은 헤더였습니다.
프롬프트 머리는 `1girl, solo` + **"A detailed anime illustration of a girl at the center."** 로,
`mature female`은 맨 끝의 무가중 태그 하나였습니다. 앞에 선 자연어가 화풍을 결정하기 때문에
"girl"이 "mature female"을 묻어버립니다. 실측(그림을 VLM에게 보여주고 인지 나이를 물은 값):

| 시트 (실제 나이) | 헤더가 "a girl" | 헤더가 "a mature woman" |
|---|---|---|
| 38세 · 흑발 업스타일 | 23.2세 | **29.5세** |
| 42세 · 흑발 웨이브+안경 | 28.8세 | **31.2세** |
| 28세 · 갈색 긴머리 (성숙 표기 없음) | 15.5세 | 15.5세 (그대로 — 요구가 없으면 손대지 않습니다) |

그래서 시트가 나이대를 요구할 때만 헤더의 명사를 바꿉니다 (`a girl` → `a mature woman` / `an elderly woman`).
태그로 밀어붙이는 방식은 두 번 실측해 본 결과 못 미쳤습니다 — 헤더 가중 태그 `(mature female:1.4)` 만으로는
오히려 −1.6세, 문장에는 `milf`를 붙여도 +(여섯 살 대신) 세 살 반 오르는 데 그쳤습니다.
남는 오차(38세 → 29.5세)는 화풍 LoRA 쪽 소관으로 보고 있습니다.

- 기준은 "30대를 30대로 뽑자"가 아니라 **'너무 어리지 않으면 통과'**입니다. 러브코미디 화풍은 본디
  젊게 나오므로, 프로브는 성숙·노년 시트에 마지노선(성숙 25세 / 노년 45세)만검사합니다. 미달하면
  이 정책이 어딘가에서 죽었다는 뜻입니다.
- 기본 ON. `--no-age-voice` 로 끄면 예전처럼 항상 "a girl"입니다.
- `--age-voice-tag` 는 문장 대신 헤더 가중 태그를 쓰는 실험용 스위치입니다(권하지 않습니다).

### 눈동자 색 — [AAA EYES]

"컷마다 눈 색이 달라진다"는 증상을 쫓아 프롬프트를 뒤져보니, 주인공 눈동자 색이 **이미지 프롬프트에
한 번도 들어가 있지 않았습니다**(0회). `config.eye_color`는 컷 스크립트 LLM에게 설명을 읽어줄 때만
쓰였고, `[AAA HAIR]/[AAA FACE]/[AAA BODY]/[AAA CLOTHES]` 로 이어지는 정체 절 목록에 눈 절이 없었습니다.
그래서 시트가 갈색 눈을 시켜도 모델이 알아서 그린 색(실측: 주황·보라)이 컷마다 나왔습니다.

시트 속성 → 정체 순서대로 읽히는 **머리 바로 아래**에 `[AAA EYES]` 를 넣었습니다. 실측(4구도 × 같은 시드):

| 시트 38세 · 흑발 | 프롬프트에 눈 색 | 본 눈동자 색 |
|---|---|---|
| 넣기 전 | 없음 | 주황 ×4 — 요구 색 0/4 |
| `[AAA EYES] brown eyes` (무가중) | 있음 | 갈색 ×4 — **4/4** |
| `[AAA EYES] (brown eyes:1.4)` (기본) | 있음 | 갈색 ×4 — **4/4** |
| 같은 태그 + 헤더 문장에도 한 번 더 | 있음 | 갈색 ×4 — 4/4 |

가중치를 아무리 올려도(1.0/1.4/1.8/2.5) 결과가 같았습니다 — **문제는 영향력이 아니라 존재**였습니다.
기본은 가중치 1.4로 두었습니다(캐릭터 태그가 다른 눈 색을 끌고 오는 회차에서 공짜 보험입니다).

- 기본 ON. `--no-eye-tag` 로 끄면 도입 전 동작(눈 색 미주입)으로 돌아갑니다.
- 가중치는 `--eye-weight 1.4`(1 이하면 무가중 태그), `--eye-prose` 로 헤더 문장에도 한 번 더 말할 수 있습니다.
- 눈동자 색은 컷 상태 시트(`_state.eyes`)가 회차 값보다 앞섭니다 — "콘택트를 벗었다" 같은 컷 지시가 먹습니다.

### 512 토큰 창 게이트

Anima는 조건 벡터를 **512 슬롯**으로 패드합니다. 프롬프트를 여러 섹션으로 적든 한 줄로 적든 결국
CLIPTextEncode 하나에 통째로 들어가므로, 512을 넘기는 순간부터 태그 하나의 영향력이 균등하게 쪼그라듭니다.
“태그를 심었는데 그림에 안 나온다”는 증상의 1차 원인이 여기 있습니다.

그래서 `anima_gen`은 큐에 넣기 직전에 프롬프트를 **추정 토큰으로 재고**(가중치 태그는 자릿수를 더 먹습니다),
**예산을 넘긴 컷만** 잘라냅니다. 우리 만화 프롬프트는 원래 짧은 편이라 대부분은 아무 일도 없이 지나갑니다.

| 순서 | 하는 일 | 이유 |
|---|---|---|
| ① | `(tag:1.0~1.3)` 을 맨 태그로 강등 | 가중치는 한 갈래 경로에서만 먹습니다. 괄호는 자리만 차지해요 |
| ② | 한글 잔존 제거 · 나이대 태그 압축 · 좌/우 좌표 문장 제거 | 자리만 먹는 토큰, 그리고 좌/우 서술은 화면 분할(split screen = 두 칸 착각)과 묶여 있습니다 |
| ③ | 예산 초과 시 `style`/`lighting`(필요하면 배경) 섹션과 긴 서술문 폐기 | 컷의 동작·인물·화각이 우선입니다 |
| ④ | 그래도 넘으면 뒤쪽 태그부터 절삭 | 머리(인원·화질·앵글)와 앞부분은 버리지 않습니다 |

- 예산은 430토큰(512 대비 마진 포함)입니다. `--no-token-gate`(또는 `ANIMA_NO_TOKEN_GATE=1`)로 끄면
  그대로 보냅니다. 무엇을 잘랐는지 로그에 남습니다 — `[TOKEN WINDOW] 추정 908 → 428 토큰 (예산 430/창 512) · style/lighting 섹션 2 · 서술문 9`.
- `--anima-close-framing` 을 켜면 사람이 보이는 컷의 `wide shot`·`full body` 를 `upper body` 로 바꿉니다
  (캐릭터가 작게 나오는 구도 억제). 사람 없는 배경·전경 컷은 전경이 목적이므로 건드리지 않습니다.
  페이지 템플릿이 전신을 요청한 칸까지 함께 바뀌니, 실측 DB의 전신 요청을 살리려면 기본(OFF)을 쓰세요.

### 2인 이상 가이드를 태그 위주로

`data_comfyui/prompt_multi.md`(2인 이상 컷의 프롬프트를 LLM이 조립할 때 쓰는 설명서)가
"and doing […]", "and the outfit features […]" 같은 **빈칸 채우기 문장형**이어서, 그 형태가 그대로
이미지 프롬프트에 실렸습니다(실측 한 회차: `and`로 시작하는 줄 122개, 설명서 구분선 `---` 40건).
 태그 모델에게 접속사는 의미가 없고 토큰 창(512)만 먹습니다. 태그 위주로 다시 썼습니다.

| 바꾼 것 | 이유(실측) |
|---|---|
| Subject 본문은 **한 줄**에 쉼표로 잇고, 줄 머리를 `and`로 시작하지 않는다 | 122줄이 문장을 잘랐다 |
| 설명서의 `---`를 출력에 쓰지 않는다 | 40건이 그대로 프롬프트에 실렸다 |
| 캐릭터 태그는 `the girl1 is <태그>` 자리. `has` 뒤에 넣지 않는다 | `has`에 두면 그림이 두 사람으로 갈라졌다 16컷 |
| 손·얼굴 등 **몸 일부가 보이면 사람**이다(`no humans` 금지) | 얼굴 클로즈업에 `no humans`가 붙어 얼굴이 사라졌다 |
| 상대방은 고정 실루엣 그룹 + 자세/행동뿐, **옷 절 금지** | `and is wearing sportswear` 잔해가 회색 천 덩어리로 렌더 |

한 개념 안의 `and` 결합("white knit top and off-shoulder top")은 그대로 둡니다 — 이건 태그를 완결되게
만듭니다. LLM이 규칙을 또 어기면 정리단계(3-8p 이전부터 있던 `_tidy_prompt`)가 같은 형태를 되돌립니다.
`1인칭(POV)` 컷은 원래 이 형태(`prompt_pov.md`: 쉼표 태그 목록 + 가중치)였으므로 그대로 두었습니다.

### 정면 구도 · 노출 램프 · 헤더 한 줄

**① 정면 구도 90% 이상 (`--straight-on`, `config.comic_straight_on`)**
"만화니까 똑바로 바라보는 느낌"이라는 지시에 따른 정책입니다. 실측 EP1이 `side_view` 17컷 /
`front_view` 2컷이었고, 원인은 `facing=right`을 `camera=side_view`로 올리던 오래된 규칙이었습니다.
`facing`은 **글칸 쪽**을 고르는 값이지 구도를 돌리는 값이 아닙니다.

- 컷 스크립트 규칙 4-b/12번: `camera`는 기본 `front_view`(행동 컷)·`close_up`(초상 컷), `side_view`·`back_view`는 회당 1~2컷.
- 응답을 어기면 `comic_gen._enforce_straight_on`이 예산(기본 10%)을 넘는 옆모습을 `front_view`로 되돌리고
  `facing=front`로 고며, pose에서 `looking to the right`·`tilted head` 같은 각도 어구를 지웁니다.
- 남길 옆모습은 (회차|컷번호) 해시로 고릅니다 — 같은 원고는 같은 그림을 그립니다. POV 컷은
  상대방이 독자를 마주보므로 정면으로 계산합니다. 배경만 그리는 확립 컷은 대상이 아닙니다.
- 컷에 `_straight_on` 표가 남아, 본문이 `she is looking at viewer`를 고릅니다(wide라고 예외 아님 —
  wide는 글칸만 오른쪽).

**② 노출은 시간에 따라 (`--exposure-ramp`, `config.comic_exposure_ramp`)**
"1화 처음에서는 옷을 잘 입고 있는 것이 일반적"이라는 지시입니다. 회차 태그셋은 회차 **전체**를 보므로
`exposure`가 후반 상태(`topless` 등)로 적히기 쉽고, 그 한 값을 모든 컷에 붙이면 컷 1부터 반라가 됐습니다.

- 태그셋이 `exposure`(**회차가 시작하는** 복장)과 `exposure_late`(**후반** 복장)을 **두 칸으로** 나눕니다.
- `anima_gen._cut_exposure(episode, cut_depth, climax, first_cut)`가 컷마다 붙일 노출을 고릅니다.
  판별 근거는 두 개입니다 — ① **회차 수위 등급** `config.review_safety[ep]`
  (`safe | sensitive | nsfw | explicit`, `_cap_safety`가 로컬 허용 여부를 확인해 explicit를 막습니다),
  ② **컷의 위치**(회차의 절반 · 클라이맥스 이후만 `exposure_late`). 회차의 첫 컷은 등급과 무관하게
  노출 어구를 걷습니다. `safe` 회차는 노출 태그를 아예 쓰지 않습니다.
- 상대방 옷 상태(`[BBB EXPOSURE]`)도 같은 기준을 따릅니다.

**③ 프롬프트 헤더는 두 줄 — 인원 태그와 앵글은 한 줄에 (`strip_duplicate_counters`)**
`strip_duplicate_counters`가 예전에는 헤더 머리의 `1girl, solo,`를 지우고 앵글에 들러붙은 중복을
남겨서, `… explicit, from_side` 줄과 `1girl, solo.` 줄이 갈라졌습니다. 이제 머리를 정답으로 두고
앵글 뒤의 중복을 지웁니다 → `1girl, solo, score_9, …, explicit, from_side` 한 줄, 그 다음 줄부터 본문.

### LLM이 깨뜨린 JSON

양자화 모델(Q4 GGUF)은 문장이 아니라 **토큰**을 뽑므로, 가끔 키 자리를 다른 글자로 디코딩합니다.
`--special` 10화 실측에서 5화가 이 한 자리(`units`의 `"cuts"`) 때문에 통째로 버려졌습니다.
그래서 세 겹으로 막습니다.

| 순서 | 어디서 | 무엇을 |
|---|---|---|
| ① | 요청 설계 | 항목 1:1 모드(기본)는 `cuts`를 **애초에 부탁하지 않습니다** — 값이 항상 1이고 코드도 그 값을 버리므로, 가장 자주 깨지는 자리를 없앴습니다 (`--no-item-cuts`는 1~2를 쓰지만 '모르면 생략'을 허용합니다) |
| ② | `comic_input.json_soft_fix` | 파싱에 실패한 응답만 관대하게 복구 — 키 자리의 `*cuts*:`·비ASCII 접두·따옴표 실종, 배열 안에 키 없이 던져진 본문 문장(`{"at": …}`로 감쌈), 객체 키 자리의 비문 삭제. **값 안의 “곡선 따옴표”와 줄임표는 건드리지 않습니다**(살아있던 값을 죽이던 실수가 여기 있었습니다) |
| ③ | `comic_input.fold_extract_keys` | 파싱은 **성공**했는데 키 이름만 찢어진 경우(`"eye_ ей_color"`, `"eye_com_color"`)를 스키마 키로 되돌립니다. 이건 조용히 '빈 항목'이 되어 LLM 호출을 한 번 더 태우고 이상한 키(체크포인트에 남습니다)는 체크포인트에 남습니다 |

그래도 안 되면 위 3-10c(체크포인트 → 빈 항목만 재확인 → 추론)가 이어받고, 그래도 모자라면
`--no-strict-state`로 경고만 켜고 가시거나 원고를 조금 더 잘게 나눠 주세요.


---

### 회차 제외 사정

`--special --all-eps` 10회차 실행을 로그로 훑어 고친 것들입니다. 산출물이 `EP01·03·04·10` 4개뿐이었는데 로그에는 **왜 6개가 없는지**가 없었습니다.

| 증상 | 조치 |
|---|---|
| 컷 스크립트 게이트로 죽은 회차(EP09)가 콘솔에만 남고 로그엔 시간만 건너뛰어 있었고 | `run_comic.perr()` — 판단 실패는 콘솔과 `log/error.log` **양쪽**에 남깁니다 |
| 페이지가 없는 회차가 '면책'인지 '미실행'인지 읽을 수 없다 | 실패한 회차는 `comic/bookNNN/episode_NN_SKIPPED.txt`로 사유·시각·원고를 남깁니다(다음 실행에 성공하면 자동 삭제) |
| 전 회차 요약이 `실패 rc=2` 한 줄만 | 이제 `완성 4회차 EP01, EP03… / 스킵 6회차 EP02(추출 실패…)`처럼 번호로 세고, 스킵은 error.log에도 적습니다 |
| 추출이 통째로 실패하면 체크포인트가 없어 재실행이 0부터 | `save_extract_failure()` — 실패 사유를 같은 원고 지문으로 보관하고, 다음 실행에 "지난 실행에서 이 원고의 추출이 실패했습니다"로 알립니다 |
| map-reduce 창이 빠진 회차가 조용히 컷을 잃었다 | `⚠ 추출 창 3개 중 1개가 비었습니다 — 그 창 사건은 컷 배분에서 빠집니다` |
| selftest 서브프로세스가 **돌아가는 중이던** 실행의 로그를 지웠다 | `log/.run.lock`(PID) — 살아있는 실행이 있으면 본 로그를 초기화하지 않고 이어 쓰고, error.log에 동시 실행을 명시합니다. 에러 줄에도 `[pid NNN]`을 붙여 섞여도 귀속을 읽습니다 |
| error.log '에러 20건' 중 13건이 프롬프트 덤프(`[PLOT_PROMPT]` 한 줄) | 덤프 헤더(`[PLOT_PROMPT] [PLOT_RESULT] [TOKEN] [ELAPSED] [TIMEOUT] [LLM release]`)는 에러로 복제하지 않습니다 |
| `--special` 어댑터가 원문의 ~40%를 지우고도 비율이 안 보였다 | 입력 로그를 `본문 1620자(원문 2673자, 어댑터 정리 -39%)`로 고쳤습니다 |
| 회차 경계의 `llama-server terminated (signal: killed)`를 크래시로 읽었다 | **의도된 동작**입니다 — 렌더가 끝난 회차는 LLM을 내려야 ComfyUI가 VRAM을 씁니다(16GB 실측). 다음 회차 추출에 ~5초 다시 올립니다. `--dry-run`은 렌더가 없어 이 언로드를 하지 않습니다(`comic_gen`의 반납 코드는 `do_render` 안) |
| `컷 예산`·`레이아웃`이 서로 다른 두 값을 한 이름으로 불러 로그를 못 읽었다 | `본문 항목 N개 / 만들 컷 N / 페이지 슬롯 N`으로 이름을 분리했습니다 |

- 확인: `comic/bookNNN/episode_NN_SKIPPED.txt`, `log/error.log`의 `===== RUN … pid=NNN =====` 구분자와 `[pid NNN]` 줄.
- `--special` 입력의 머리/꼬리 블록(제목·요약·"다음 화 예고") 정리량은 위 표 마지막 줄대로 매 회차 찍힙니다 — 원고가 통째로 잘려 들어가는 건 아닌지 이 숫자로 판단하세요.

### 컷 분할을 원작 쪽으로 넘기고, 컷 시트를 정시로 만들었습니다 (`cutsheet.py` · `comic_cut_gen.py` 삭제)

**왜 넘겼나.** `comic_cut_gen.py`는 두 저장소에 같은 파일(984줄)로 복사되어 있었고 우리는 한 번도
import하지 않았습니다(참조 0·셀프테스트 0). 그런데 그 안의 생각 네 가지는 원작 생성기가 이미 실행해
놓고 있었습니다 — 형제 저장소 `comic/`에 `ep01_<해시>.txt`(컷 시트) · `episode_01_cuts.json`(컷 색인) ·
`episode_01_assets.json` · `summary.txt`(감사)가 실제로 빠져 있었습니다. 반대로 우리 사본
(`inputs/progress_3rd`)에는 장면 카드가 **0장**이라 `_special_specs` 경로가 아예 안 도와 원작이 장소를
지어내는 셈이었습니다. 그래서 "코드는 안 나누고 **파일만** 나눈다"로 갈았습니다.

**실측한 위험.** 컷 시트 해시는 `ae7853edbd894547`, 같은 폴더 시트 해시는 `7c0d2b13e3d44b3f` — 서로 다른
실행이 한 폴더에 섞여 있었습니다(원작 생성기가 진행기를 리셋했기 때문입니다). 컷 수는 회당 **198컷**
(우리 페이지 템플릿은 한 면 2~6컷). 이 두 가지가 새 배선의 근거가 되었습니다.

**결정(사용자 지시).** ① 컷 시트는 **구조화**(`episode_NN_cuts.json`)를 정본으로, 텍스트 사본은 폴백으로
② 한 회차는 **12면** 예산(면당 5컷 ≈ 60컷) ③ 스탠딩 이미지는 **우리가 새로 그려** 재사용한다(원작 PNG를 얻어오지 않는다) ④ `comic_cut_gen.py`는 삭제하되 **상태카드 만드는 법은 여기로 이식**한다.

**그래서 한 일.** `cutsheet.py`가 컷 시트 한 행을 `comic_gen._special_specs()`가 이미 읽는 행 모양
({tag,text,line,act,who})으로만 바꿉니다 — 매핑 로직을 두 곳에 두지 않았습니다. 카드 행은 추출 프롬프트의
장면 카드({장소,상황,시간,복장,복장2,복장 주인})로 다시 묶고, 컷 시트 맨 앞 카드 묶음은 **회차 시작 카드**로
대우합니다(그래야 회차 `clothes`·배경의 1순위 근거가 됩니다). `inherited` 카드는 버리고, `TALK2/INNER2`는
화자 이름을 붙여 `TALK/INNER`로 내리고, `STANDING`은 전신 스탠딩 자리로 강등해 `standing` 깃발을 남기고,
`PAGE_TURN`은 버리고, `split_part`(이미 분리된 발화)는 다시 쪼개지 않습니다. 형태 강등은 모드 공통이고
`--headers-mode min|rec|full`은 "무엇을 버리나"만 조절합니다.

**면수 예산을 서술 컷부터 자르는 이유.** 대사는 그 자체가 원작이고, 말풍선은 폭 25%·글자 하한(≈11px)으로
더 줄일 데가 없습니다. 그래서 장면 전환 카드는 거의 전부, 발화는 대부분, 서술은 남은 자리만큼 살리고 자리 전체에서
균등 간격으로 고릅니다(한 막이 통째로 사라지지 않게). 실측: EP01 198컷 → 60컷(12.0면), 막별 20/16/10/14,
장면 카드 9장·분리 발화 조각 9개 보존. 사연은 `comic/bookNNN/audit_epNN.json`에 남깁니다.

**정규화는 한 곳으로.** 원고 구분선 제거가 세 곳(`comic_input.strip_markdown` · `novel_progress.DIVIDER_RE` ·
`comic_cut_gen.DIV_H_RE`)에 있어 원고 포맷이 바뀔 때마다 세 곳을 고쳐야 했습니다. 사본을 삭제해 둘로 줄였고,
`selftest`가 "원고 정규화가 한 곳에 있다"로 다시 늘어나는 것을 막습니다.

### 전신 한 장을 두 곳에서 쓰도록 했습니다 — 새 모듈 없이, 정본의 스탠딩 어구를 그대로

사용자 지시는 두 개였습니다: **스탠딩 이미지는 원작 PNG를 얻어오지 않고 우리가 새로 그린다**, 그리고
**`standing_gen.py` 같은 새 파일은 필요 없다 — `anima_gen`에 스탠딩 함수가 이미 있다.** 정본
`llm_shortnovel_generator_gui/anima_gen.py::anima_gen_standing`을 끝까지 읽어 보니 핵심은 새 코드가 아니라
[ANGLE] 슬롯 대체문이었습니다 — `date_sim:2.0, front_shot:2.0, white_background:2.0, no background,
tachi-e.` + front_shot, negative에 `multiple people, clones, split view, collage, from above:3.0`,
해상도 슬롯 6(1024×1344). 우리 `anima_gen`에도 같은 슬롯 표가 이미 있었고요. 그래서 상수 네 개와 분기 하나로
끝냈습니다(포즈 문장 사전 `story/UNCOMMON/standing_pose.txt`는 가져오지 않았습니다 — 만화는 컷마다 pose를
가지고 있는데 랜덤 포즈가 그걸 덮어버리기 때문입니다).

흰 배경 처리는 합성 쪽 일입니다. 단순히 "흰색 = 투명"으로 하면 흰 셔츠·흰 양말까지 사라지므로, 1/4 축약본에서
**테두리 기원 flood-fill**로 '바깥'만 찾고 LINEAR로 되돌려 테두리를 부드럽게 처리했습니다. 실측(EP01 실제 렌더):
스프라이트 1024×1344, 배경 `[254,255,255]`, 모서리 alpha 평균 0.0 / 인물 중심 255.0, 불투명 비율 23.6%,
인물 세로 전체(0~1343) — 전신 한 장이 화면 가득 잡혔습니다.

정직한 예상: 절감 폭은 **전신 컷 비율**이 상한입니다(268면 실측: 전신 14%·상반신 38%·클로즈업 18%). 그래서
기본은 OFF(`--standing-reuse`)로 두고, 한 회차에서 렌더가 몇 장 줄어드는지 본 뒤 기본값을 정겠습니다.
'배경 컷 재사용'을 함께 넣은 이유는 여기 있습니다 — 전신 컷 한 장이 스프라이트 1장 + **이미 렌더된 배경**으로
채워지면, 배경을 두 번 그리는 일이 없어져 절감이 비율보다 커집니다.


### 컷 시트를 우리 손으로 만들어 입력 폴더에 두기로 했습니다 (정본 `comic/` 의존을 끊었습니다)

배포판에는 정본 경로가 없습니다. 그래서 참고할 파일을 **입력 폴더의 세 가지**(시트 JSON · `ep{NN}_<해시>.txt` ·
`episode_NN_reviewed.md`)로 못 박고, 컷 시트(`cuts_epNN_<해시>.json`)는 실행이 만들어 **그 폴더에** 저장하게
했습니다. 근거: 실측으로 `ep03_7c0d2b….txt`에 헤더가 **44건**([LOCATION]/[SITUATION]/[TIME]/[CLOTHES]/
[CLOTHES2]/[ACTION]/[TALK]/[INNER]) 이미 들어 있었습니다 — 컷의 뼈대는 원작이 갖고 있고, 본문을 보며 살을
붙이는 일만 남 있습니다.

**정제는 "재작성"이 아니라 "조정"입니다.** LLM에게 컷 목록을 다시 쓰라고 하면 원작 대사가 살이 붙어 변합니다.
그래서 네 가지 조정만 받습니다: `split`(긴 발화 두 컷 — 두 조각을 이어붙여 원문과 같으면 채택, 다르면 폐기),
`insert`(본문이 정당화하는 컷 — 대사 문장은 받지 않음), `speaker`(화자 확정), `card_skip`(안 바뀐 장면).
실측(EP03): 전사 47컷 → 정제 **53컷**(분리 5 → 조각 10, 컷 추가 1, 화자 확정 13, 카드 생략 0), 원고 해시
`be1c129f52a73e51`, 53컷 ≈ 10.6면(목표 12면 안이라 잘림 없음).

**재사용 판정은 "파일 존재"가 아니라 원고 해시입니다.** ①②③의 sha256를 `meta.source_sha`로 박아, 본문이
 바뀌면 자동으로 녹슨다(`stale()` → 재생성). 강제 재생성은 `--rebuild-cutsheet`, 아예 안 만들기는 `--cutsheet off`.
파일명에는 실행 해시를 넣어 옆의 시트·본문과 같은 실행분인지 기존 검사가 그대로 작동하게 했습니다.

**함께 고친 것.** LLM 응답에서 JSON을 꺼낼 때 `\}.*$` 정규식을 썼는데, 안 쪽 객체의 `}`에서 잘려 조정이
조용히 전부 버려졌습니다(실측: 응답에 분리 3건이 있었는데 정제 0건). 문자열을 무시하는 중괄호 깊이 스캔으로
바꿨습니다. 입력 폴더에 출력을 쓰므로 `.gitignore`에 `inputs/**/cuts_ep*_*.json`을 넣었고(진행기 사본은
35개 파일이 추적 중이라 안 넣으면 매번 dirty), 쓰기는 tmp→replace(반쪽 파일이 남지 않습니다).


### 발화는 반드시 눕히고, 박스는 글자 크기대로 재고,풍선은 최대한 위로 (사용자 지시 3칙)

**① 강제 줄바꿈(`must_wrap`).** 대화·속마음이 한글 2단어 이상, 또는 한글 5자 이상의 1단어이면 무조건 두 줄
이상으로 접습니다(`_wrap_balloon_lines`가 폭을 0.85→0.42까지 좁혀 다시 감고, 그래도 안 되면 음절 경계 절반에서
자릅니다). 근거: 말풍선 폭은 컷의 25%까지인데 한 줄로 길면 FONT_FLOOR(11px)까지 줄어야 하고, 그러면 화면에서
안 보입니다. 실측: "계속 이렇게 있을 수는 없어요…" → 3줄, "나 좋아?" → 2줄, "좋아"(1단어 3자) → 1줄 유지.

**② 박스는 지금 글자 크기 기준으로.** 예전 코는는 컷 대비 25% 상한을 넘으면 **박스를 사후 축약**해서(×0.86 등)
글자보다 박스가 작아지는 잘림이 생겼습니다. 이제 박스를 줄이지 않고 **글자 크기를 한 단계씩** 줄여 다시 재고,
최소 글자(11px)까지 왔으면 그 크기로 감수합니다. 그래서 "박스 ≥ 글자"가 항상 성립합니다(검사으로 고정).

**③ 최대한 위로.** 자리 4칙(위/중간 4칸)에 더해, 한 칸을 정한 뒤 같은 열 안에서 **y를 컷 맨 위까지 올려**
보고 안 겹치는 첫 자리에 놓습니다(`_raise_up`). 실측(700×460, 18px): 상대방 발화 (513,16)=오른쪽 위,
주인공 발화 (16,16)=왼쪽 위, 면적 2.1~4.8%.


### LoRA를 실행 머리에 찍고, 컷 스크립트를 재사용하고,벡터 풍선에 꼬리를 붙였습니다

**LoRA 출력(사용자 지적).** LoRA 확정 값은 렌더 직전 `anima_gen.log`의 `[ComfyUI LoRA]` 한 줄에만 남았고,
`--dry-run`이면 아예 보이지 않았습니다. 그래서 `anima_gen.lora_report()`를 만들어 해석 결과를 실행 머리에
한 줄로 찍고 `comic_gen.log`에도 복제했습니다(키 이름은 파일명에서 역참조 — `lora_lambton(Lambton.safetensors)`).
겸사겸사 로컬 래퍼의 `set -u` 사고를 고쳤습니다: 기본값을 준 자리(`${VARIANTS:-3}`)와 달리 뒤에서 `${VARIANTS}`를
맨 참조해 "unbound variable"로 죽었습니다(실측: `go 3`이 실행 라인만 찍고 종료).

**컷 스크립트 재사용.** `episode_NN_comic.json`에 그 회차의 스크립트(panels/page_plans/beats/target_panels)가
이미 들어가 있었다면, 원고 도장만 없었을 뿐입니다. 컷 시트 도장(`cutsheet.sheet_sha`)을 같이 박고,
`run_comic.cached_script()`가 같은 도장이면 그걸 `script=`로 넘깁니다 → **컷 스크립트 LLM 0회**, 같은 컷 구성으로
재렌더. 도장이 다르거나(`--rebuild-cutsheet`, 원고 수정) 없으면 평소대로 LLM이 씁니다. 끄는 법 `--no-reuse-script`. 재사용 전 필수 항목(`type/pose/camera/caption_ko/clothes/emotion/position/lines`)을
검사해 하나라도 빠지면 **조용히 이상해지는 대신** 재사용을 포기합니다(실측: 손으로 만든 스크립트에 `pose`가 없어 `_base_seed`에서 KeyError — 이제 "재사용 포기: 컷 1에 필수 항목이 없습니다"로 알린다). 실측 재현: 같은 원고·같은 도장으로
다시 돌려 `◆ 컷 스크립트 재사용[episode_03_comic.json]: 컷 1개 · 컷 스크립트 LLM 0회` → 렌더(최고점 72점) → 페이지 1장 완성.

**벡터 풍선 꼬리.** 자산 모드에는 꼬리가 굽혀 있지만 벡터에는 없어 화자 표지가 없었습니다(사용자 지시).
말풍선은 삼각형, 속마음은 생각 물방울 2개이고, 방향은 자산과 같은 규칙 — 컷 가운데(화자) 쪽으로 아래로.
컷 바닥이 가까우면 짧아지고, 꼬리까지 포함한 상자가 겹침 검사에 쓰입니다(실측: 주인공은 박스 오른쪽끝 부근,
상대방은 왼쪽 끝 부근, 길이 20px).


### 화자 점으로 풍선을 앉히고, 후보를 정리하고, 전 화차 컷 시트를 만들었습니다

**꼬리를 화자로(사용자 지적).** 꼬리를 붙였지만 방향이 **풍선이 앉은 열** 기준으로만 정해졌습니다. 그래서
컷 스크립트가 이미 아는 사람 자리(`position`)로 화자 점을 만들고(`text_payload`가 풍선마다 `aim`을 실음),
① 풍선을 그 사람 위 열에 앉히고 ② 꼬리를 그 점으로 내립니다. 실측(700×460 컷): 주인공이 컷 오른쪽(사람 x=0.70)에
서면 풍선이 왼쪽(x=16~187)에 떠 꼬리만 대각선이었는데, 이제 풍선이 오른쪽(x=513~684)으로 옮겨가고 꼬리 x가
513~544로 사람 쪽을 향합니다. 한가운데/자리 모르면 화자 규칙을 그대로 씁니다(회귀 방지).

**탈락 후보 정리.** best-of-N을 켜면 후보가 컷당 N-1장씩 매일 쌓입니다. `comic_gen.cleanup_rejected()`를 만들어
실행이 끝날 때마다 돌립니다: 같은 컷은 최신 1장 · 7일 지난 것 · 전체 200MB 초과분(오래된 것부터). 실측 픽스처
(한 컷 후보 3장=최신/1.2일/9일 + 다른 컷 1장=10일) → 3장 삭제·1장 보존, `--rejected-keep-per-cut 0/2` 동작 확인,
`--dry-run`이면 견적만(지우지 않음).

**평가기에 컷이 아는 정보를 넘겼습니다.** 552장 코퍼스에서 8장을 뽑아 채점하니 평균 74.2점(30~92)이었고,
낮은 두 장이 **사람 없는 배경 컷**에 사람 기준을 들이댄 결과(30점)였습니다. `_pick_best_shot`이 `people`/`camera`를
안 넘기고 있어서 넘기도록 고쳤고, 같은 이미지를 `people=none`으로 다시 채점하니 **30점 → 82점**으로 올라갔습니다.
(채점 실패가 렌더를 막아선 안 된다는 규칙은 그대로입니다.)

**전 화차 컷 시트 사전 생성.** `cutsheet.py --build`를 붙여 EP01~EP10을 한 번씩 만들었습니다(EP11은 원고 캐리어가
없어 정상 거부). 화차별 35~61컷 → 7.0~12.0면. 이제 어느 화차를 돌려도 컷 시트 생성 LLM이 0회이고, 원고 해시가
 바뀌면(`source_sha`) 그때만 1회 돕니다. 참고로 `sources()["sheet"]`은 **캐릭터 시트**를 가리키는 키라 컷 시트가
아닙니다(첫 배너에서 "업스트림 시트 있음"으로 오판한 원인 — 스킵 근거에서 제외).



### `run_local.sh all`이 전 화차를 "페이지 0장"으로 건너뛰었습니다 (실측 사고 12건)

원인은 어제 붙인 **컷 스크립트 재사용** 배선이었습니다. 재사용을 못 찾으면 `{}`를 돌려주는데, 그 빈 사전째로
`script={}`로 넘겼습니다. `comic_gen_episode`는 `if script is None:` 에서만 LLM을 부르므로 빈 스크립트가 그대로
통과 → `panels` 없음 → **`컷 스크립트 없음 → 건너뜀`** → rc=1. 화차마다 `episode_NN_SKIPPED.txt`만 쌓였습니다.

세 가지를 고쳤습니다(실측 EP04로 검증).
1. 호출부는 `script=(_script or None)`, 함수는 `if not script:` — 빈 스크립트는 없는 스크립트입니다(이중 안전).
2. 실패 사유를 구분했습니다 — 컷이 0장이면 "렌더는 했는데 합성된 페이지가 없음"이 아니라
   **"컷 0장 — 컷 스크립트를 만들지 못해 렌더를 하지 않았습니다"** 로 적습니다(`_why_code`). 예전 문장이
   렌더를 안 한 회차에 '렌더는 했는데'라고 알려줘서 원인을 역방향으로 훑게 만들었습니다.
3. 재사용 검사 목록이 지나치게 엄격했습니다 — 저장되는 패널에는 `emotion`이 없습니다(표정은 `state/_state`로 돕니다).
   없는 키를 요구해 **실제 산출물에서 재사용이 영 불발**되고 있었습니다(요구 키: type/pose/camera/caption_ko/position/lines).

검증: EP04 실재실행(컷 시트·`--lora1 lora_sex --str1 0.8`) → 컷 10장 렌더 → 페이지 3장 완성(exit 0, 942초),
그 직후 같은 산출물로 재사용 판정 → **발동 · 컷 10개 · 근거 episode_04_comic.json**. 실행에 남은 에러는 본문
추출 JSON 파싱 1회 재시도(통상) 한 건뿐이었습니다.



### "주인공과 상대방의 태그가 섞이는데?" — 섞인 게 아니라 **주인공 외모가 비어 있었습니다**

실측 프롬프트(EP2 컷48): `[Subject 1: girl1] the girl1 is zero two (darling in the franxx), **short dark hair and
straight bangs**, (green eyes:1.4) …, **muscular body** …` — 시트는 `dyed pink hair / long wavy hair / gyaru /
curvy / large breasts / tanned skin`인데 시트 속성이 하나도 없고, 시트가 말하지 않은 속성만 있습니다.
추적 결과 **혼입이 아니라** **대체**였습니다. 태그 블록을 조립할 때 컷 상태 시트 값을 시트 값 *대신* 넣었습니다:
`hair_line = _st.get("hair") or f"{config.hair_color}, {config.hair_style}"` — 상태가 `ponytail` 한 단어를 주니
시트의 머릿색·길이가 통째로 사라졌고, 가이드 템플릿은 머리칸을 요구하니 LLM이 자기 기억(zero two=짧은 흑발)으로
채웠습니다. `body`도 같아서 상태의 `slim` 이 시트의 `curvy` 를 밀어내고 "muscular body"를 불렀습니다.
(상대방은 예전부터 "고정 그룹 그대로 쓰고 지어내지 말라"는 장치가 있어 덜 섞였고, 주인공엔 그 장치가 없었습니다.)

고친 것 4곳:
1. 상태 시트 프롬프트 — 체형·머리색·머리길이·피부색을 **다시 쓰지 말라**고 명시(예시도 `slim body` → `sweat, flushed skin` 으로 교체).
2. `_merged_attr()` — 시트 고정 속성을 항상 앞에 두고, 상태 문구에서 **시트가 이미 말한 범주**의 재서술만 버립니다(범주가 비어 있을 때만 상태가 채운다). 단어 경계로 봅니다(`standing` ≠ `tan`).
3. `[AAA SKIN]` 신설 — 상태 문구의 피부·질감 글자를 체형 자리에서 떼어냅니다.
4. 가이드(pov/multi)·요청 문구 — "외모는 받은 대로만, 줄이 없으면 빼라"를 주인공에게도 상대방과 같은 등급으로.

실측 전/후(같은 화차 EP2, 컷 6장/페이지 1장 완료):
- 전: `short dark hair and straight bangs, (green eyes:1.4), muscular body`
- 후: `dyed pink hair and long hair and wavy hair and gyaru and messy hair, (green eyes:1.4) and sparkling eyes, curvy body and large breasts and wide hips, tanned skin and flushed skin, sweaty`
- 상대방은 그대로 고정 그룹: `the boy1 is (gray silhouette:5.0) and (featureless:5.0) tall man, …`(옷·행동만 덧붙음)



### 이모티콘을 무료 이미지로 크고 예쁘게, 그리고 [CLOTHES]가 처음 몇 장에 안 반영되던 버그

**이모티콘(사용자 지적: "하트·화남·느낌표 아이콘이 너무 작고 예쁘지 않음").** 예전은 19px 벡터로
그려서 작고 밋밋했습니다. 무료 이모지 **Twemoji(CC BY 4.0)** 7종을 `--get-emotif`로
`data_comfyui/emotif/{kind}.png`에 받아 두고, 있으면 그것으로 **46px·투명도 그대로** 붙이고, 없으면
벡터(26px, 예전 19보다 크게)로 돌아갑니다. `--emotif-style auto|image|vector`로 제어.
실측: heart 박스 46px(이미지) vs 30px(벡터). 자산 7장은 커밋에 넣었습니다(data/balloons처럼).
`surprise`(❗ 187B)는 단순 이모지라 수신 임계값(200B)에 걸려 실패했는데 150B로 낮췄습니다.

**[CLOTHES]가 처음 몇 장에 반영 안 됨(사용자 지적: p05까지).** 추적 결과 **섞인 게 아니라
상태 보충 LLM이 지어낸 것**이었습니다.
- 추출은 [CLOTHES] 카드를 정확히 반영했습니다 → `config.clothes = "white off-shoulder knit top, …"`(정답)
- 태그 블록 조립은 `if _ovr: base_clothes = _ovr` — **컷 상태의 복장이 비어있지 않으면 회차 기준 복장을 통째로 덮어씀**
- 그런데 `fill_missing_state`(상태 보충 LLM)는 pose·지문·직전 상태만 받고 **회차 시작 상태(`config.clothes` 등)를
  받지 못해** 컷 1~5의 `clothes`를 `formal business attire`(은행원 직업을 보고 지어낸 것)로 채웠고, 그것이 정답 복장을 덮었습니다
  (place도 `Shionzawa bank counter`, time도 `night`로 지어졌습니다 — 추출 출력에 `location`/`time_of_day` 키가 아예 없었음)
- 고침 ① `fill_missing_state`가 `base_cut_state`(장면 카드 1순위)를 `[회차 시작 상태(원작 지정)]`로 함께 보내
  원작이 정한 값이 있으면 그대로 쓰게 함 ② 추출이 [LOCATION]/[TIME] 카드의 장소·시간을 `location`/`time_of_day`로
  출력 ③ `apply_to_config`가 그것들을 `config`에 담음
- 실측(모킹): 보충 프롬프트에 `place=school gymnasium; time=dusk; clothes=white off-shoulder knit top, …`가 들어감 확인
- 셀프테스트 함정 하나: `apply_to_config`가 이제 `config.location`을 쓰게 되자, 위 테스트가 511줄에 세운
  "living room" 픽스처가 630/635줄 `apply_to_config`에 덮여 폴백 검증이 죽었습니다 → 검증 직전에 픽스처를 되세팅



### variants 렌더 시 이름표가 기본값 "standing"으로 빠지던 버그

**사용자 지적: "standing 이미지에 캐릭터가 나와야 하는데 배경만 나옴."** 추적 결과 스탠딩
스프라이트가 아니라 **배경 establishing shot의 variants 2·3호가 이름표 "standing"으로** 저장된
것이었습니다.
- 원인: `render_panel`이 이름표(`anima_nametag`)를 **루프 밖 한 번만** 세우고, `finally`에서
  **매 반복마다** 기본값(`anima_gen.anima_nametag = "standing"`)으로 리셋했습니다. 그래서
  variants 1호만 정확한 이름표(`comic_e1_p01_wide_establishing_`)를 쓰고, 2·3호는 "standing"으로
  저장됐습니다(실측: p01 시드 136439163 → "standing" 136440172(+1009)·136441181(+2018)).
- 고침: 이름표·회차를 **루프 안 매 후보마다** 다시 세우게 했습니다(후보는 같은 컷이므로 이름표도 같아야 함).
- 기존 잘못 이름 붙은 파일(`image/rejected/episode_{1,3}_standing_anima__00002_.png`)은 삭제했습니다.
- 셀프테스트: variants=3 렌더(모킹)에서 3호 모두 같은 정확한 이름표(`comic_e1_p07_…`)를 쓰고 "standing"이 아님을 검증



### 로컬 전용 시스템 프롬프트 (system_prompt / system_prompt_anima)

사용자가 "local에서 돌릴 때에만" 특정 시스템 프롬프트를 쓰라고 지정했습니다.
- `config.apply_local_settings`가 `local_settings.yaml`의 `system_prompt`/`system_prompt_anima`를
  읽도록 했습니다. 키가 있으면(=로컬) 그것으로, 없으면(=커밋만 있는 환경) 기본값("") 그대로 —
  **값은 `local_settings.yaml`(gitignore)에만** 두어 커밋 코드엔 안 남습니다.
- 반영 지점: `config.system_prompt`는 메인 LLM 10개 호출(추출·컷 스크립트 등, `openAPI_control`이
  `system_prompt=None`일 때 `config.system_prompt`로 대체), `config.system_prompt_anima`는
  `anima_gen`의 ANIMA 태그 추출(system 메시지에 "danbooru image tags as strict JSON"을 덧붙여).
- 지정값: 두 프롬프트는 본문이 같고 마지막 줄만 다름 — `system_prompt`는 "Please answer in Korean.",
  `system_prompt_anima`는 "Please answer in English." (일본 라이트노벨 스타일·무분별 순응 지시).
- 셀프테스트: apply_local_settings 로 값을 심으면 채워지고, 키가 없으면 ""로 리셋되지 않음을 검증



### 이미지 생성과 평가를 분리 — ComfyUI·LLM 메모리 경쟁 제거

사용자 제안: "이미지 생성-평가가 번갈아 되니, ① LLM 전체 언로드 ② ComfyUI로 이미지 전체 생성
③ 필요한 이미지만 LLM에서 추출"하면 메모리 경쟁이 없을 것. 확인 결과 **variants>1이면 컷마다
"생성→평가"를 번갈아** 해서 ComfyUI가 돌고 있는 중에 평가 LLM이 다시 올라와 VRAM/RAM을 경쟁했습니다.

- `render_panel`에 `pick_best` 파라미터 추가 — False면 **생성만** 하고 후보 전체를 리스트로 반환(채택 X).
  True(기본)면 예전처럼 생성+채택(호환 유지).
- 렌더 루프는 `pick_best=False`로 모든 컷의 후보 전부 생성(ComfyUI만, LLM은 이미 언로드).
  `got[i]`는 후보 목록(리스트). `_latest_bg_image`도 목록 처리.
- 렌더·재전송이 **모두 끝난 뒤** 배치 평가: 후보 2장 이상인 컷만 `_pick_best_shot`(LLM)으로 채택,
  나머지는 `image/rejected/`. `got[i]`는 최고 1장으로 바꿈.
- 효과: 렌더 구간엔 ComfyUI만, 평가 구간엔 LLM만 → 경쟁 없음(ollama·vLLM 모두).
  vLLM은 모델이 VRAM 상주라 배치화만으론 상주 VRAM이 안 빠짐 — local은 `--gpu-memory-utilization`
  (예: 0.5)을 함께 낮추면 실질적 해결.
- 셀프테스트: `pick_best=False`가 후보 전체를 리스트로 반환하고 평가(LLM)를 안 함을 검증



## 세부 변경 기록 (README에서 옮긴 줄들)

- > **중요 — 에피소드 중단 문제 해결 [2026-09-08]**
- [2026-09-08] 이전 버전에서 `run_ollama.sh`가 들고 있던 값들
- [2026-09-08] 이전 배선은 ① 본문을 기승전결 **가이드 4문장**으로 압축 ② 레이아웃이 컷 수(≤4페이지/24컷)를
- | ① 본문 → 컷 수 | `comic_input.target_panels_from_weights()` (2026-09-09) | **사건(액션) 단위** 합산 — 평범한 사건 1컷, 신체 접촉·관계 변화·클라이맥스는 2컷. 하한 `max(6, 막당 2컷)`, 상한 `--max-panels` |
- - **[2026-09-08] 자수가 적으면 기승전결 '막(幕)'이 장면 골격이 됩니다**: 추출 LLM은 가이드와 함께
- **[2026-09-09] 컷 배분의 저울을 '글자 수'에서 '일어난 사건'으로 옮겼습니다**
- (`본문 항목` / `만들 컷` / `페이지 슬롯`은 서로 다른 값입니다 — 예전 로그의 `컷 예산`·`레이아웃`이 이 두 이름을 섞어 썼기 때문에 2026-09-10에 분리했습니다.
- **[2026-09-08] 기승전결 전달 보강** — 기승전결 분해는 언어모델에게 그대로 맡깁니다(한글 능력 신뢰).
- [2026-09-09] 기본 동작입니다. 추출 단계에 본문을 **일어난 시간 순서대로 화면 항목 하나씩**으로 나눠 달라고 시킵니다. 항목은 셋 중 하나입니다.
- [2026-09-09] 배분은 원래 **완전 결정론**입니다(같은 입력 → 같은 컷 수, 같은 페이지 구성, 같은 표정). 재현성을 위해 그렇게 만들어 두었는데, 그래서 같은 원고를 두 번 뽑으면 화면이 똑같았습니다.
- - **설명 박스 폭(2026-09-09)**: 길이가 길면 컷 폭을 따라 자랍니다 — 평범 컷 **80%**까지, 대사가 있는 컷 **62%**까지, **★지문은 컷 폭 전체**를 씁니다(넓어야 줄 수가 줄어 글자가 안 잘립니다). 짧은 지문은 측정한 글자 폭만큼만 남긴다(컷을 빈 박스로 안 채웁니다).
- - **풍선은 컷 대비 25%로 (2026-09-15 사용자 지시: "대화창/속마음 창이 너무 크다 — 컷(!) 대비 25%")**: 폭 비율 `BALLOON_W_RATIO`/`THOUGHT_W_RATIO = 0.25`이고, 몸통 **면적** 상한 `BALLOON_MAX_AREA_RATIO = 0.25`(컷 면적의 25%)를 따로 두었습니다. 폭만 줄이면 세로가 늘어 면적이 다시 커지므로(실측: 몸통 보스트 2.2 × 최소 높이 42% → 컷의 절반), 면적 상한이 먼저 이깁니다(넘치면 글자 단계를 더 줄입니다).
- - **꼬리(화살표) 방향 — 컷 왼쪽에 놓이면 반대로 (2026-09-15)**: 풍선이 컷 **왼쪽** 열에 있으면 인물이 그 오른쪽/아래에 서 있으므로 꼬리는 **오른쪽 아래**로 굽혀집니다. 오른쪽 열은 실측으로 방향이 맞으므로 그대로입니다.
- - **풍선 가로 폭은 컷의 25% (2026-09-10: 20%→10% · 2026-09-11: 15% · 2026-09-14: 30% · 2026-09-15: 25%)**: 말풍선이 얼굴을 덮지 않게 폭을 컷 폭의 비율로 고정하고, 대사를 그 폭에 맞춰 접어 세로로 늘립니다(글자 크기도 줄여서) · 속마음 타원의 세로는 '그 폭에 글자를 넣는 데 필요한 최소'만 씁니다(세로가 컷 높이의 36%를 넘으면 폭을 더 넓혀 줄 수를 줄입니다)
- - **컷당 풍선은 최대 3개입니다(2026-09-11: 사용자 지시 2→3).** 이 상한은 `comic_page_merge.BALLOON_MAX` **하나**에서 나오고, 컷 스크립트 상한(`comic_gen.DIALOG_LINES`)이 같은 값을 따릅니다 — 한쪽만 올리면 대사가 3번째 풍선 전에 잘려 화면에 나타나지 않습니다.
- - **설명은 자르지 않습니다(2026-09-09)**: 지문은 길이로 잘지 않고(옛 `…` 토막 마감 폐지), 길면 박스가 자라고 그래도 안 들어가면 **글자 크기를 20px → 최소 11px까지 줄여** 다 담습니다. 실측: 132자=20px·6줄, 600자=19px·32줄, 3,000자=11px·90줄(3단 세로컷 기준, 전부 잘림 없음)
- - ★ 지문은 원문의 **줄바꿈·빈 줄(문단 띄움)을 그대로 반영**합니다(2026-09-14). 다른 지문·풍선은 개행을 공백으로 취급합니다(`wrap_text(keep_lines=True)`는 ★자리에서만 동작).
- [2026-09-09] 주신 20종(구 `test/cut_new.yaml` — 병합 후 정리)을 기존 14종에 더해 **34종**이 되었습니다.
- [2026-09-09] 실측: 뽑아낸 페이지의 표정이 전부 **같은 극단 표정**으로 고정돼 있었습니다. 원인은 화면 문법이 아니라 **태그 배선**이었습니다.
- [2026-09-09] 실측: 이벤트가 아닌 p02 컷이 **옷 없이** 찍혔습니다. 그 컷에 보낸 프롬프트 끝이 이랬습니다.
- **[2026-09-13] 방향성 자산 18종** — 같은 몸통을 왼쪽용(`_l`)·오른쪽용(`_r`)로 두 장 굽니다.
- **꼬리 모양 (2026-09-14 지시)** — **1:1.3 직각삼각형**이고, 짧은 변에는 검정 선을 그리지 않고 그 변을
- `alpha`(플레이트 불투명도 — [2026-09-14]부터 **255 = pure white**), `moods`(감정 매칭용 영문 태그),
- - **플레이트는 pure white, 테투리는 검정 불투명**으로 구워져 있습니다(2026-09-14 지시로 반투명을
- [2026-09-09] 실측: 시트에 `#Kirisaki Chitoge from Nisekoi#`를 넣었더니 주인공 이름이 'AMD 소녀'에서 **치토게**로 바뀌었습니다. 추출 LLM이 `#…#` 공식 캐릭터 태그를 이름 필드로 옮겨 적었기 때문입니다. `#태그`는 **그림을 그릴 때의 참조(트리거)**이고, 이름은 화면 지문·대사에 쓰이는 별개의 값입니다.
- (gray silhouette:5.0) and (featureless:5.0) <tall|short|muscular|heavy-set> man ← 기본(2026-09-15)
- **기본은 회색 실루엣입니다(2026-09-15 사용자 실측).** 같은 컷(EP1 컷 25)에서 상대방을 `his`로만 두면
- `# 주인공 (…)`, `--- 본문 ---`)만 걷어 않고 원고를 그대로 올립니다**(2026-09-13 사용자 지시: 원고를 손으로
- **장면 카드** — 원작이 장소·상황·시간·복장을 직접 지정해 주는 블록입니다(2026-09-11 입력 변화). 회차 시작에 4개가 한 덩어리로 오고, **기승전결 중간에도 필요하면 들어옵니다**(장면·복장이 바뀌는 지점).
- | `[TALK]`→`이름: 대사`, `[INNER]`→`(속마음) …` | 원작에는 화자 표기가 없어 컷의 화자가 뒤바뀝니다. 추정은 **① 발화 안의 호명 → ② 직전 서술의 주어(`렌이 … 관찰한다`) → ③ 교대 순서** 순서입니다. ①만 쓰면 이름 없는 발화에서 첫 마디부터 어긋나 두 발화가 통째로 뒤바뀌었습니다(2026-09-11 실측) |
- > **2026-09-11 수정**: 추출 프롬프트의 `[에피소드 N 본문]` 블록이 한때 "# 본문 미사용" 주석으로

## 커밋 연대기

| 날짜 | 커밋 | 제목 |
|---|---|---|
| 2026-09-17 | `c1fa696` | 2인 가이드를 태그 위주로 다시 쓴다 — and 줄·구분선·has 자리·no humans·옷 절을 규칙으로 박다 |
| 2026-09-17 | `16dfe57` | 같은 컷을 3장 뽑아 최고점만 쓴다 — 그리고 log/tag_out.txt 에서 세운 결함 5건 |
| 2026-09-17 | `f3e4b2d` | LoRA 는 키로 고른다 — 로컬 엔트리에 lora 목록 대조와 go 의 LoRA env, 그리고 파일 칸에 들어가던 trigger 하나 |
| 2026-09-17 | `ae3ebf4` | 지금 상태 기준 한 방 명령어 — README 3-0절, 그리고 배너가 "34종"이라 거짓말하던 것 |
| 2026-09-17 | `ecf7d23` | 실런 시트의 'green eyes,' 쉼표 꼬리표도 태그 하나로 정리된다(눈 색 절 견고성 검사) |
| 2026-09-17 | `2458756` | 컷마다 눈동자 색이 달랐다 — 원인은 프롬프트에 눈 색이 한 번도 없었던 것 |
| 2026-09-17 | `6e9809a` | "너무 어리지 않으면 통과"를 검사로 박는다 — 프로브에 인지 나이 마지노선 |
| 2026-09-17 | `3e5b617` | 서른여덟이 23세로 그려졌다 — 헤더가 "a girl"이면 mature female 은 묻힌다 |
| 2026-09-17 | `1cd9c2c` | 닮은 캐릭터 태그, 실렌더로 검증 — 속성으로 안 잡히는 생김새에서 교차-시드 판정이 NO→YES |
| 2026-09-17 | `1dd3acf` | 미시 요청에 20대 얼굴이 오면 안 된다 — 나이대 가중을 3배로, 유사도는 0~1로 정규화 |
| 2026-09-17 | `6a5f6af` | 얼굴이 회차를 넘겨 흔들린다 — 시트 속성으로 닮은 캐릭터 태그를 고른다 (106종 실측 DB) |
| 2026-09-16 | `a8c4e91` | Anima는 512 슬롯만 본다 — 정본에서 토큰 창 게이트를 이식하다 |
| 2026-09-16 | `252e1f9` | 컷 레이아웃을 268면에서 배운다 — cut_new.yaml(실측 20종) + 칸별 생성 요청 gen 배선 |
| 2026-09-15 | `b0f1a75` | 그린 컷을 LLM이 채점한다 — image_eval.py (사용/재생성 판정) + prerun 실측 |
| 2026-09-15 | `ee9e66b` | 만화는 정면 · 상대는 실루엣 · 노출은 램프 — [CLOTHES2/3] 카드, 풍선 면적 25%, 헤더 한 줄 |
| 2026-09-13 | `b2765e3` | 확립 컷 지문 한 줄 요약 / [CLOTHES] 자막 없음 / 풍선 가로 30%·이미지 기본 / ★지문 줄 띄움 / 썸네일 옵트인 |
| 2026-09-13 | `b772ceb` | --special: 헤더 → 컷 1:1 매핑 ([L][S][T]=그림 한 장 / [CLOTHES]=전신+승계 / [ACTION]=큰 장면 / [INNER][TALK]=portrait 하나씩) |
| 2026-09-13 | `7ba287f` | ★지문 = 원작 prologue/epilogue 전문 출력 + [INNER]/[TALK] 평면형 기기 인식 보수 |
| 2026-09-13 | `a1cfcec` | --special: progress/ prologue_/epilogue_ 원문을 ★지문 근거로 인식 (+ 로컬 LLM 오버레이) |
| 2026-09-13 | `62b3b54` | 말풍선에 방향성 도입 — 자리 4칙, 발화 3개+ 사전 분리, ★ multi-line, 플레이트 pure white |
| 2026-09-12 | `6cf12d2` | 성별이 프롬프트에 새지 않게 — 대명사 / 수위 태그 중복 / 남자 하체 / 남×남 anal |
| 2026-09-12 | `e36e71f` | 화풍 디테일러 기본 OFF(--detailer) + 워크플로우 덤프를 드래그 가능한 API 그래프로 |
| 2026-09-12 | `93c6c00` | LoRA 디버깅: 제출 직전 최종 워크플로우를 회차당 1장 남깁니다 |
| 2026-09-11 | `70b404d` | 풍선 3개 · 폭 15% · 큰 지문 25px — 개수 상한의 출처를 BALLOON_MAX 하나로 |
| 2026-09-11 | `76c128c` | 합성 게이트(컷 전부 렌더된 회차만) + 상대방 고정 태그(invisible man) 이식 |
| 2026-09-11 | `6d0e225` | 렌더 2종 교정: 잘린 PNG 사본 검수 + LaTeX로 감싼 값 복구 |
| 2026-09-11 | `bbe2a00` | --special 장면 카드 신형 입력([LOCATION]/[SITUATION]/[TIME]/[CLOTHES]) 대응 |
| 2026-09-10 | `7b0f2ce` | --special 10화 실행 분석으로 나온 6가지 결함 수정 (JSON 견고화 + 실패 가시성) |
| 2026-09-10 | `f5eb13e` | 대화창·속마음창 폭 절반 (컷 20%→10%) — 최소 폭 96→48, 안쪽 여백 12→8, 구제 폭 28%→14% |
| 2026-09-10 | `f208018` | 말풍선 꼬리(화살표)·속마음 화살표·생각 물방울(작은 원) 폐지 — 몸통만 그린다(사용자 지시) |
| 2026-09-10 | `38e738f` | 말풍선 2.2배(글자가 몸통 밖으로 넘치던 것) + 꼬리·물방울 자산화(회전) + 정책 무관 selftest |
| 2026-09-10 | `93d10d5` | 말풍선·속마음 이미지 은행 (--balloon-style image) — 9슬라이스 + 감정 선택 + 벡터 폴백 |
| 2026-09-10 | `1c1dbcc` | 로그는 실행 시작에 초기화 + 에러는 error.log로 따로 + 추출 체크포인트(빈 항목만 이어서 채우기) |
| 2026-09-10 | `792dd59` | README·flow: 상대방(BBB) 상태 시트와 첫 컷 2회 확인을 문서에 반영 |
| 2026-09-10 | `c097174` | 상대방(BBB) 상태 시트 — 두 사람이 한 화면인 컷에서 상대도 '지금'을 가진다 |
| 2026-09-10 | `1b52b59` | flow.md: JSON 파싱 실패 재시도 요약 추가 (추출 2회·태그 2회·컷 스크립트 장면당 2회) |
| 2026-09-10 | `a2aae0c` | JSON 파싱 실패 재시도를 3곳에 넣었다 (추출·태그 생성·컷 스크립트 기본값) |
| 2026-09-10 | `f6c17ee` | flow.md: 엄격 검사 게이트·JSON 구제·규칙 순서 사고를 고장표에 반영 (3-4 섹션 번호 정리) |
| 2026-09-10 | `272c939` | 규칙 순서 오염 복구 + 컷1 상태는 본문 근거만 + JSON 모자라면 에러 종료 + 기본 pose standing |
| 2026-09-10 | `e173d87` | 컷 연속 상태에 장소·시간·배경 추가 (place / time / background) |
| 2026-09-09 | `b8634c3` | 컷별 연속 상태 시트 — 표정/메이크업/몸/복장/악세사리(+머리·흔적·소지품·자세), 미언급은 직전 컷 유지 |
| 2026-09-09 | `ae26503` | 컷 1부터 아헤가오·빔보 복장 — 회차 태그의 '시간'을 가렸다 |
| 2026-09-09 | `7391ad7` | 항목 20개가 컷 6개로 눌리던 것 고정 — 항목 저울을 막 분할에서 독립시켰다 |
| 2026-09-09 | `c183325` | LLM 디코딩 사고로 추출 JSON이 죽지 않게 — 키 앞 따옴표가 U+2024로 바뀌는 실측 케이스 복구 |
| 2026-09-09 | `e791f72` | 페이지 템플릿을 하나로 고정 --template (1종=회차 전체 통일, n종=페이지마다 회전) |
| 2026-09-09 | `5693f96` | flow.md: LLM 호출 경로 정정 — #2 태그 생성은 openAI_response(mainLLM) 경로, textLLM이 아니다 (본문 원문은 안 보내고 가이드만) |
| 2026-09-09 | `36715fd` | flow.md — 현재 동작 파이프라인 정리 (오프라인 리뷰용) |
| 2026-09-09 | `cdda656` | 본문 항목 1개 = 컷 1개 (행동/대사/속마음) + 레이아웃이 목표 컷 수를 담도록 고정 |
| 2026-09-09 | `01fbde9` | 프롬프트 정제 로그를 회차 끝 한 줄로 요약 ( 일은 한다, 소음은 버린다 ) |
| 2026-09-09 | `a197240` | 컷 크롭을 얼굴 중심으로 (--no-face-crop으로 회귀) |
| 2026-09-09 | `4538641` | 컷 템플릿 DB 14종 → 34종 (주신 20종 병합) + 같은 용도でも 다른 컷을 쓰는 추첨 |
| 2026-09-09 | `d54f95a` | 컷 배분에 변동(--variation/--vary) + 모든 컷 하단 설명('수다장이' 모드) + 극단 표정/과노출 어휘를 로컬 어휘 사전으로 이동 |
| 2026-09-09 | `0fb2274` | docs: README.md에서 트러블슈팅 표를 제거하고 README_local.md로 옮긴다 |
| 2026-09-09 | `440eb71` | feat: 주인공·상대방 이름 고정 (--name / --name2, COMIC_PIN_NAME, local_settings name) |
| 2026-09-09 | `6577ac2` | fix: 컷 1컷이 누드로 찍히던 문제 — 회차 의상 기준도를 컷까지 유지한다 |
| 2026-09-09 | `beb1120` | fix: 표정이 전부 아헤가오로 고정되던 문제 — 표정을 컷 단위로 바꾼다 |
| 2026-09-09 | `ee4b831` | feat: 풍선 가로 비율 20%·세로로 길게, 설명·대사는 자르지 않고 폰트를 줄여 담는다 |
| 2026-09-09 | `de652ea` | fix: ★큰 지문 글자를 줄이고 박스를 컷 폭 전체로 넓힌다 (사용자 지시 2건) |
| 2026-09-09 | `a46da44` | feat: 컷 배분의 저울을 '본문 글자 수'에서 '일어난 사건(액션)'으로 옮긴다 |
| 2026-09-09 | `12248dc` | docs: --no-epilogue/--no-summary-cuts 도움말을 새 ★규칙에 맞게 수정 |
| 2026-09-09 | `1079c88` | feat: ★큰 지문을 3곳으로 축소(프롤로그/회차 앞/마지막 회차 에필로그) + 설명 박스 미잘림 + 글리프 폴백 |
| 2026-09-09 | `3964e91` | feat: Gaegu(OFL 손글씨 붓체)를 화면 문법 폰트로 연결 |
| 2026-09-09 | `d1581f9` | feat: 화면 문법 v4 — 화자별 풍선 자리·초소형 꼬리, 설명 박스 크기 절약, 경계선 밀착, 감정 이모티콘 |
| 2026-09-09 | `e376af3` | llm_comic_gen — 평문 → LLM 추출 → ComfyUI/Anima 만화 생성 |

> 이 표는 `git log --pretty=format:'%ad|%h|%s' --date=short` 를 그대로 옮긴 것입니다.
> 항목별 사정(실측 근거)은 위쪽에, 동작 설명은 `README.md`에 둡니다.

### (추가로 옮긴 기록)

**2026-09-11 수정**: 추출 프롬프트의 `[에피소드 N 본문]` 블록이 한때 "# 본문 미사용" 주석으로
빠져 있었습니다. 프롬프트는 "시트와 본문을 읽고"라고 말하면서 본문은 없으니, `units`의 `at`이
본문에 없는 문장이 되어 막 분할이 글자수 균등 분할로 후퇴하고 `guides`는 지어졌습니다. 복원했고
selftest가 "본문이 프롬프트에 들어있는지"를 이제 검사합니다. 본문은 `episode_char_budget()`
안에서만 자르고, 그 예산은 **시트 길이까지 뺀 값**입니다(`episode_char_budget(sheet_text=…)`) —
시트가 예약 토큰을 먼저 먹으면 본문 때문에 num_ctx를 넘어 ollama가 프롬프트 앞부분(규칙·스키마)을
버립니다. 실측 시트 2,600자 → 본문 예산 11,145자에서 9,701자로 줄어듭니다.

### 캐릭터 태그의 괄호 — anima는 괄호를 가중치로 읽는다

Danbooru 캐릭터 태그는 동명이인 구분자로 괄호를 씁니다(`zero_two_(darling_in_the_franxx)`). Anima/SDXL
계열은 프롬프트의 `(...)` 를 **가중치 구문**으로 해석하므로, 그대로 보내면 안쪽 글자만 가중치로 소비되고
캐릭터가 빠집니다. 그래서 선택기가 돌려주는 태그와 작품 태그는 `(` `)` 를 `\(` `\)`로 바꿔 실습니다
(`chara_match.prompt_safe`). 밑줄→공백 치환과 순서는 무관합니다 — 어느 쪽이 먼저여도 결과는 같습니다.

### 닮은 캐릭터 후보가 여러 개일 때 — 동률 랜덤

속성 하나로 캐릭터가 하나만 정해지지 않습니다(흑발 롱헤어는 후보가 수두룩). 최고점 하나를 고집하면
같은 시트에서 같은 얼굴만 반복되므로, 최고점과 유사도 0.05 이내 후보를 풀로 보고 그중 뽑는 옵션을 넣었습니다.
기본은 기존 동작(최고점 하나). `--char-pick random --char-topk 3 --char-seed <해시>` 로 켜고, 시드를 주면
같은 시드 → 같은 선택으로 재현됩니다. 로그에 풀이 그대로 남습니다:
`[[CHAR TAG]] … → **albedo_\(overlord\)** … — 동률 후보 3개 중 뽑음 [boa_hancock, albedo_\(overlord\), akemi_homura]`.
